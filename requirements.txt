# ─────────────────────────────────────────────
# AI Email Assistant Harness — Requirements
# ─────────────────────────────────────────────

# CLI
typer[all]>=0.12.0
rich>=13.7.0

# Configuration
pydantic>=2.0.0
pydantic-settings>=2.0.0

# LLM
openai>=1.30.0           # OpenAI GPT-4o / GPT-4o-mini

# Gmail API
google-api-python-client>=2.100.0
google-auth>=2.22.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.2.0

# Database (async)
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.19.0        # SQLite async driver (local dev)
# asyncpg>=0.29.0        # Uncomment for PostgreSQL in production

# HTTP client (used by LLM SDK)
httpx>=0.27.0

# Dev & testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
